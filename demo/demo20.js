// Create all permutation of a string?
// var str = 'abc';
var arr = ['a', 'b', 'c', 'd'];
function permutation(arr) {
	var len = arr.length;
	var ret = [];
	var arrtemp = arr.splice(0, 1);
	for(var i = 0; i < arrtemp.length; i++) {
		// arr[i] = per[i];
		// console.log(arr);

	}
}
permutation(arr);

// var obj = Object.create(arr);
// console.log(obj.__proto__);